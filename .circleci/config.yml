
#jobs:
#  build:
#    docker:
#      - image: circleci/<language>:<version TAG>
#    steps:
#      - checkout
#      - run: <command>
#  test:
#    docker:
#      - image: circleci/<language>:<version TAG>
#    steps:
#      - checkout
#      - run: <command>
#workflows:
#  version: 2
#  build_and_test:
#    jobs:
#      - build
#      - test
jobs:
  standard_make:
    steps:
      - run: make
workflows:
  version: 2
  build_and_test:
    jobs:
      - standard_make