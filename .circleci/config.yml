
#jobs:
#  build:
#    docker:
#      - image: circleci/<language>:<version TAG>
#    steps:
#      - checkout
#      - run: <command>
#  test:
#    docker:
#      - image: circleci/<language>:<version TAG>
#    steps:
#      - checkout
#      - run: <command>
#workflows:
#  version: 2
#  build_and_test:
#    jobs:
#      - build
#      - test
jobs:
  standard_make:
    docker:
      - image: koalaman/shellcheck-alpine:stable
    steps:
      - type: shell
        shell: /bin/bash --login
      - checkout
      - run: make install
workflows:
  version: 2
  build_and_test:
    jobs:
      - standard_make