# Use the latest 2.1 version of CircleCI pipeline processing engine, see https://circleci.com/docs/2.0/configuration-reference/
version: 2.1

job:
  job_test: # The job name
    machine: true # executor
    steps:
      run:
        make
workflow:
  version: 2.1
  workflow_test: # workflow name
    jobs:
      job_test